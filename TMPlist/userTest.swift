//
//  userTest.swift
//  TMPlist
//
//  Created by taemmy on 2021/01/28.
//  Copyright © 2021 tm. All rights reserved.
//

import Foundation
/*
let plist = UserDefaults.standard

plist.set("홍길동", forKey: "이름")
plist.set(24, forKey: "나이")
plist.set("남", forKey: "성별")

plist.synchronize()//   데이터 읽을때

let plist = UserDefaults.standard

let name = plist.string(forKey: "이름")
let age = plist.integer(forKey: "나이")
let sex = plist.string(forKey: "성별")
 
 let sex = plist.string(forKey: "성별") as? NNString
 //1. 입력된 키에 맞지 않은 데이터 -> 2. 데이터가 있더라도 nil !! --> 잘못된 타입 지정으로 인한 캐스팅 실패
  
 as? --? ???
 
 
*/
